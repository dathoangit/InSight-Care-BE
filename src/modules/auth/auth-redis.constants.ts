/** Single source of truth for Auth-related Redis key prefixes and TTLs. */
export const AUTH_SESSION_KEY_PREFIX = 'auth:user-session';

export const AUTH_USER_CACHE_KEY_PREFIX = 'auth:user';

export const AUTH_USER_CACHE_TTL_SECONDS = 30;
